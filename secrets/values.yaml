# secret main file

# will use affix to automatically fill some hosts.
affix: "FIXME"

auth:
  auth: "oidc"
  authIdToken: "FIXME"
  authVO: "panda_dev"

  oidc:
    audience: "https://pandaserver-doma.cern.ch"
    config_url: "https://panda-iam-doma.cern.ch/.well-known/openid-configuration"
    clients:
      - name: "panda_dev"
        client_id: "FIXME"
        client_secret: "FIXME"
      - name: "Rubin"
        client_id: "FIXME"
        client_secret: "FIXME"
      - name: "Rubin.production"
        client_id: "FIXME"
        client_secret: "FIXME"


msgsvc:
  enabled: true

  # if it's empty, affix will be used to create a default server.
  serverList: []
  #  - "activemq-dev:61613"

  basePasswd: "FIXME"

  # accounts
  accounts:
    - "panda"
    - "jedi"
    - "idds"
    - "pilot"

  # message channels. name:write_accounts,read_account
  channels:
    - "idds_panda:idds:panda,jedi"
    - "panda_idds:panda,jedi:idds"
    - "self.jedi_taskstatus:jedi:jedi"
    - "panda_jedi:panda:jedi"
    - "panda_pilot:panda:pilot"
    - "idds.internal:idds:idds"


panda:
  enabled: true

  database:
    password: "FIXME"
    # affix will be used to create a default dbhost if it's empty
    dbhost: ""

  hostName: ""

  port: "80"
  sslPort: "443"
  # affix will be used to create default urls if it's empty.
  url: ""
  urlSSL: ""
  monitorURL: ""
  cacheURL: ""
  verifyHost: "off"
  configRoot: "/tmp/"

harvester:
  enabled: true
  experiment: ""

  # harvester id
  harvesterID: "my-harvester"

  database:
    password: FIXME
    admin_password: FIXME
    # affix will be used to fill dbhost if it's empty
    dbhost: ""


idds:
  enabled: true

  # affix will used to create a default restHost if it's empty
  # restHost: "idds-dev-rest"
  restHost: ""
  restPort: 8443

  database:
    name: "idds_db"
    user: "idds"
    password: "MyPass123"
    schema: "doma_idds"
    # affix will be used to fill host if it's empty
    # host: "idds-dev-postgres"
    host: ""


bigmon:
  enabled: true


iam:
  enabled: true

  database:
    # affix will be used to fill host if it's empty.
    # host: "iam-dev-mariadb"
    host: ""
    name: "iam_db"
    user: "iam"
    password: "MyPass0508"
    admin_password: "MyAdminPass0508"

  cilogon:
    client_id: "FIXME"
    client_secret: "FIXME"


rucio:
  rucioHost: "sprucio01.sdcc.bnl.gov:443"
  authHost: "sprucio01.sdcc.bnl.gov:443"
  username: "iddsv1"
  password: "FIXME"
  account: "iddsv1"
