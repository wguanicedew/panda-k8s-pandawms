# Default values for panda.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

jedi:
  persistentvolume:
    class: nas
    create: false
    selector: false


server:
  persistentvolume:
    class: nas
    create: false
    selector: false

  route:
    enabled: true

  httpsProxy: "http://proxy.sdcc.bnl.local:3128"
  httpProxy: "http://proxy.sdcc.bnl.local:3128"
  noRoot: true
  hostName: "sphenix-pandaserver.apps.rcf.bnl.gov"


postgres:
  persistentvolume:
    class: nas
    create: false
    selector: false

  noRoot: true
