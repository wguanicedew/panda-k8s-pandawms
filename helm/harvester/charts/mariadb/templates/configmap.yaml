apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mariadb.fullname" . }}-custom
  labels:
    {{- include "mariadb.labels" . | nindent 4 }}
data:
  my_custom.cnf: |-
    {{- .Values.customConfig | nindent 4 }}
